<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Etude: ${etude_name} - ${etude_description}</title>
    <style>
        body { font-family: sans-serif; margin: 20px; line-height: 1.6; }
        h1, h2 { border-bottom: 1px solid #eee; padding-bottom: 5px; }
        pre { background-color: #f5f5f5; padding: 10px; border-radius: 4px; white-space: pre-wrap; word-wrap: break-word; }
        ul { padding-left: 20px; }
        .metrics-list li { margin-bottom: 5px; }
    </style>
</head>
<body>
    <h1>Etude: ${etude_name}</h1>
    <p><em>${etude_description}</em></p>

    <h2>Metrics for Etude '${etude_name}':</h2>
    <ul class="metrics-list">
        <li>Status: <strong>${metrics_status}</strong></li>
        <li>Items Processed: <strong>${metrics_items}</strong></li>
        <li>Version: <strong>${metrics_version}</strong></li>
        <!-- Add more generic or specific metrics placeholders as needed -->
    </ul>

    <h2>Content for Etude '${etude_name}'</h2>
    <p>This is the main content area for etude <strong>${etude_name}</strong>.</p>
    <p>Further details and specific functionality for this etude will be implemented here.</p>

    <!-- Example of how other data could be injected if etude provides it -->
    <!-- <div>${etude_specific_content}</div> -->

    <hr style="margin-top: 30px;">
    <footer>
        <p><small>Content generated by the 'Trouble' static site generator for Etude ${etude_name}.</small></p>
    </footer>
</body>
</html>
